# Copilot Instructions

## Project Guidelines
- User requests to stay within Step 1 scope only: project setup, references, packages, folders, DI extension skeletons, exception middleware, appsettings, and Program.cs basic configuration with Scalar; do not add entities, DbContext, JWT configuration, or controllers.